[package]
name = "minetacs"
version = "0.1.0"
edition = "2024"

[lib]
# Crate type cdylib for wasm32 targets
crate-type = ["rlib", "cdylib"]

[dependencies]
serde = { version = "1.0", features = ["derive"] }

[target.'cfg(target_arch = "wasm32")'.dependencies]
serde_json = "1.0"
