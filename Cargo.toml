[package]
name = "minetacs"
version = "0.1.0"
edition = "2024"

[lib]
crate-type = ["cdylib", "rlib"]

[dependencies]
urandom = { version = "1.0.0-alpha.2", default-features = false }

[dev-dependencies]
clap = { version = "4.5", default-features = false, features = ["std", "help", "usage"] }
urandom = "1.0.0-alpha.2"
